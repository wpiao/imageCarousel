import styled from 'styled-components';

const bigImage = styled.img`
  top: 0px;
  left: 0px;
  cursor: pointer;
  position: absolute;
  transform: scale(1);
  max-width: 100%;
`;

export default bigImage;
